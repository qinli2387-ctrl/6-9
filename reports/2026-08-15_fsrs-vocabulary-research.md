# 任务执行报告

## 1. 任务目标

全面调研六分计划可改进点，形成有证据的优先级，并先实施学习价值最高、范围可控的一批改进。

## 2. 原始要求

- 先调研再实施。
- 产品需支持手机和电脑、云端记录及可视化闯关。
- 后续考虑类似开心消消乐的学习玩法。
- 修改后必须实际打开和验证，不能越改越差。

## 3. 修改前状态

- 代码树干净，公开演示已存在。
- 前四周关卡可玩，24 周地图可视化完成。
- 词卡数据库只有部分 FSRS 字段，没有调度接口、复习页或四档反馈。
- 正式 Sites 项目不可获取；公开演示与正式 D1 应用是两套运行链路。
- 构建此前可通过；完整 lint 会扫描 `work/` 生成物并报告大量无效错误，同时存在少量旧代码规范问题。

## 4. 执行方案

1. 审计代码、数据库、路线图、授权和部署状态。
2. 查询雅思官方标准、学习科学、FSRS、游戏化、无障碍和开源项目。
3. 将正式云端链路、摸底、复习闭环、内容覆盖排在三消表现层之前。
4. 实施 FSRS 词汇闭环和完整迁移。
5. 完成构建、lint、回归测试、手机浏览器交互测试和独立审查。
6. 更新路线图、交接文档和 GitHub。

## 5. 完成内容

- 接入 `ts-fsrs` 5.4.1。
- 新增 12 张原创雅思基础词卡。
- 新增云端到期队列、查看答案和四档回忆评分。
- 持久化完整卡片状态与复习日志。
- 到期队列清空后自动完成词汇任务并一次性奖励 XP。
- 新增正式 `/vocabulary`、复习 API 和无数据库的 `/demo/vocabulary` 验收入口。
- 修复完整 lint 的生成目录污染、内部链接和 React 纯度问题。
- 完成调研报告、路线图和交接更新。

## 6. 修改文件

- `db/schema.ts`：补齐 FSRS 卡片与日志字段和索引。
- `app/api/vocabulary/review/route.ts`：鉴权、用户隔离、FSRS 调度、日志、任务和 XP 更新。
- `app/vocabulary/*`：正式词汇复习页面与客户端交互。
- `app/dashboard/*`：显示真实到期数量并进入复习流程。
- `app/globals.css`：词卡、四档按钮和手机布局。
- `eslint.config.mjs`：排除生成目录，使 lint 结果有效。

## 7. 新增文件

- `lib/vocabulary.ts`：原创词卡种子和数据库到 FSRS 卡片转换。
- `drizzle/0003_romantic_mojo.sql`：兼容旧表的 FSRS 数据迁移。
- `app/demo/vocabulary/page.tsx`：本地交互验收入口。
- `tests/browser-vocab-smoke.mjs`：真实 Chrome 手机尺寸冒烟测试。
- `docs/RESEARCH_AUDIT_2026-08-15.md`：产品与技术调研结论。

## 8. 测试记录

- 测试：生产构建；命令：`pnpm test` 中的 `vinext build`；结果：11 个页面/API 路由构建成功；状态：✅ 已验证。
- 测试：代码规范；命令：`pnpm lint`；结果：0 error；状态：✅ 已验证。
- 测试：服务端渲染回归；命令：Node test runner；结果：1/1 通过；状态：✅ 已验证。
- 测试：手机浏览器交互；命令：Chrome DevTools smoke test；结果：查看答案、4 个评分按钮、选择“记得”、下一卡和 1/3 进度均通过；状态：✅ 已验证。
- 测试：正式 D1 写入；结果：Sites 项目不可访问；状态：❓ 尚未验证。

## 9. 测试失败记录

- 错误：首次依赖安装阻止构建脚本；原因：供应链策略需要显式允许现有 `esbuild`、`sharp`、`workerd`；修复：固定 pnpm 允许列表并设置 Node 路径；结果：构建成功。
- 错误：完整 lint 报告 2692 项；原因：扫描旧的 `work/` 构建产物，另有少量旧 React/Link 问题；修复：排除生成目录并修正真实代码问题；结果：0 error。
- 错误：一次最终浏览器复测无法显示评分按钮；原因：旧服务在重新构建后仍请求旧 chunk；修复：使用新端口启动全新生产进程；结果：冒烟测试通过。
- 错误：正式 Sites 查询失败；原因：托管服务返回 `Sites project not found`；处理：未新建、未覆盖、未发布；结果：仍为阻塞项。

## 10. Code Review 结果

- P2：迁移向已有日志表增加无默认值的非空字段可能失败；已为迁移补充兼容默认值。
- P2：所有词卡、任务、资料和 XP 查询/更新必须按用户隔离；已逐条确认 API 条件包含 `userId`。
- P3：词汇任务原来可以不复习直接点完成；已改为进入复习，清空队列后由服务端自动完成。
- P5：旧服务进程在重建后可能提供混合资源；已记录为验收注意事项。

## 11. 自动改进记录

### 第 1 轮

发现：数据表不满足完整 FSRS 卡片和日志结构。修改：扩展 schema、迁移和服务端调度。结果：构建成功。

### 第 2 轮

发现：任务可以绕过真实复习获得完成状态。修改：词汇任务改为开始入口，服务端在队列清空后完成并奖励。结果：代码审查通过。

### 第 3 轮

发现：完整 lint 被生成物污染并暴露旧 React 问题。修改：收紧忽略项并修复真实问题。结果：lint 0 error。

### 第 4 轮

发现：旧生产进程导致资源版本混用。修改：新进程重新验收。结果：手机浏览器冒烟测试通过。

## 12. 外部资料研究

- IELTS 官方格式、评分和 Band Descriptors：采用，决定四科摸底和输出技能的评价边界。
- `ts-fsrs` 官方仓库：采用，MIT 且与 TypeScript 栈匹配。
- 间隔学习与主动回忆研究：采用，支持优先完成复习闭环。
- EFL/ESL 游戏化研究：部分采用；用于参与度设计，不用于声称三消能直接提高雅思分数。
- Phaser 官方 Next.js 模板：暂不采用；等到第一版三消时再引入，避免当前增加体积和复杂度。

## 13. 安全检查

- 未发现新增 API Key、密码、Token、数据库 URL 或 `.env` 提交。
- 学习者数据读写均按服务端认证 `userId` 过滤：根据代码判断通过。
- 非本人词卡 ID 返回不存在：根据代码判断通过，生产集成尚未验证。
- 未执行破坏性数据库操作或生产发布。

## 14. 回归测试

- 首页服务端渲染测试重新通过。
- 完整构建重新通过。
- 完整 lint 重新通过。
- 前四周关卡相关文件只修复计时引用初始化和内部链接，没有改变评分或解锁逻辑；构建通过，未重新跑完整四关浏览器流程。

## 15. 未解决问题

- 正式 Sites 项目不存在，D1 迁移、账号登录和两台设备同步未验证。
- 四科摸底、动态复习关、错题分类、写作和口语尚未实现。
- 公网演示仍是浏览器本地进度，不包含本次正式 FSRS 云端功能。

## 16. 当前完成度

本次“调研并实施首批改进”约 90%。本地实现、审查和交接完成；缺少的 10% 是正式 D1 环境与跨设备生产验收，原因是现有 Sites 项目不可获取。

## 17. 下一步建议

### 建议立即做

1. 建立四科摸底和初始技能权重。
2. 恢复或明确替换正式 Sites 项目，执行迁移并做双设备同步验收。
3. 建立听力/阅读错因分类并动态生成复习关。

### 建议之后做

- 扩充第 5–8 周内容，再制作复用同一到期词卡队列的三消原型。

### 暂时不建议做

- 不建议现在复制完整 Candy Crush 或 Duolingo 仿制项目，也不建议在正式学习闭环未跑通前投入大量动画和素材。
